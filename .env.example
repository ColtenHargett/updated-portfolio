# Optional. Enables the AI-written briefing in the News Summary Agent demo.
# Without it, the demo still shows the live, most-covered stories.
GEMINI_API_KEY=
# Optional. Defaults to gemini-2.5-flash.
GEMINI_MODEL=
